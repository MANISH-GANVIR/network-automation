# =========================================================
# TERRAFORM AZURE
# =========================================================

# 4) Resource Group
resource_group = """
provider "azurerm" {
  features {}
}

resource "azurerm_resource_group" "rg" {
  name     = "demo-rg"
  location = "East US"
}
"""

# 5) VNet + Subnet
vnet_subnet = """
provider "azurerm" {
  features {}
}

resource "azurerm_resource_group" "rg" {
  name     = "demo-rg"
  location = "East US"
}

resource "azurerm_virtual_network" "vnet" {
  name                = "demo-vnet"
  address_space       = ["10.0.0.0/16"]
  location            = azurerm_resource_group.rg.location
  resource_group_name = azurerm_resource_group.rg.name
}

resource "azurerm_subnet" "subnet" {
  name                 = "demo-subnet"
  resource_group_name  = azurerm_resource_group.rg.name
  virtual_network_name = azurerm_virtual_network.vnet.name
  address_prefixes     = ["10.0.1.0/24"]
}
"""

# 6) Storage Account
storage_account = """
provider "azurerm" {
  features {}
}

resource "azurerm_resource_group" "rg" {
  name     = "demo-rg"
  location = "East US"
}

resource "azurerm_storage_account" "storage" {
  name                     = "demostorageacct123"
  resource_group_name      = azurerm_resource_group.rg.name
  location                 = azurerm_resource_group.rg.location
  account_tier             = "Standard"
  account_replication_type = "LRS"
}
"""

# =========================================================
# LIKELY VERBAL QUESTIONS
# =========================================================

ansible_questions = [
    "How do you run the playbook?",
    "What is register?",
    "Why delegate_to: localhost?",
    "Why gather_facts: false?",
    "Difference between ios_command and ios_config?"
]

terraform_questions = [
    "What do terraform init, plan, and apply do?",
    "What is the state file?",
    "What is a provider?",
    "What is a resource group?",
    "What are VNet and subnet?"
]

# =========================================================
# ONE-LINERS TO MEMORIZE
# =========================================================

ansible_one_liners = {
    "register": "Stores command or task output in a variable.",
    "stdout[0]": "Represents the first command output.",
    "delegate_to: localhost": "Runs the task on the control node.",
    "gather_facts: false": "Avoids unnecessary fact collection and speeds up execution."
}

terraform_one_liners = {
    "terraform init": "Initializes providers and plugins.",
    "terraform plan": "Shows a preview of the infrastructure changes.",
    "terraform apply": "Creates or updates infrastructure.",
    "state file": "Stores the current mapping of real infrastructure resources."
}

# =========================================================
# MOST IMPORTANT COMMANDS
# =========================================================

important_commands = [
    "ansible-playbook -i inventory/hosts.yml playbooks/backup.yml",
    "terraform init",
    "terraform plan",
    "terraform apply"
]